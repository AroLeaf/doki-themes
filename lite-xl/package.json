{
  "name": "doki-theme-lite-xl",
  "version": "0.1.0",
  "private": true,
  "main": "index.js",
  "scripts": {
    "build": "node .",
    "package": "tar -cf lite-xl.tar build && gzip lite-xl.tar",
    "install": "rm -rf ~/.config/lite-xl/colors/doki && cp -r ./build ~/.config/lite-xl/colors/doki"
  }
}
